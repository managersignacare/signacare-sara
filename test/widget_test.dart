import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — app requires Riverpod ProviderScope and async init
    expect(true, isTrue);
  });
}
